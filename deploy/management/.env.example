MANAGEMENT_IP=192.168.100.202
PLATFORM_STATE_ROOT=/var/lib/robot-platform
MLFLOW_ARTIFACT_ROOT=/mnt/robot_platform/mlflow-artifacts

POSTGRES_USER=robotplatform
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=platform
MLFLOW_DB=mlflow

# Pin images by digest after the pilot validation if the environment is frozen.
POSTGRES_IMAGE=postgres:16
REDIS_IMAGE=redis:7-alpine
MLFLOW_VERSION=3.1.1
