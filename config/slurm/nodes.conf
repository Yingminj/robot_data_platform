# Run `sudo slurmd -C` on every GPU node. Copy its NodeName line here, then:
#   1. change NodeName to mgmt01/gpu01;
#   2. add NodeAddr=<fixed-IP>;
#   3. append Gres=gpu:1 State=UNKNOWN.
# Values below are deliberately invalid placeholders and must not be deployed.
NodeName=mgmt01 NodeAddr=192.168.100.202 CPUs=32 Boards=1 SocketsPerBoard=1 CoresPerSocket=16 ThreadsPerCore=2 RealMemory=61912 Gres=gpu:1 State=UNKNOWN
NodeName=gpu01 NodeAddr=192.168.100.215 CPUs=FILL_ME Boards=FILL_ME SocketsPerBoard=FILL_ME CoresPerSocket=FILL_ME ThreadsPerCore=FILL_ME RealMemory=FILL_ME Gres=gpu:1 State=UNKNOWN
